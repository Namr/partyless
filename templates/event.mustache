<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{event_name}} | Partyless</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="/styles.css" rel="stylesheet" />
  <script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>

  <style>
    .event-card {
      width: min(100%, 1080px);
    }

    .card-pane {
      padding: clamp(18px, 2vw, 26px);
      display: flex;
      flex-direction: column;
      gap: 18px;
    }

    .event-pane {
      background: var(--mint);
      border-right: 4px solid var(--ink);
    }

    .guest-pane {
      background: var(--paper);
    }

    .eyebrow {
      font-size: 12px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
    }

    .event-title {
      margin: 0;
      font-size: clamp(24px, 3vw, 40px);
      text-transform: uppercase;
    }

    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
      gap: 12px;
    }

    .info-block {
      border: 3px solid var(--ink);
      padding: 12px;
      background: var(--paper);
      box-shadow: 6px 6px 0 0 var(--ink);
      display: grid;
      gap: 4px;
    }

    .info-label {
      font-size: 12px;
      text-transform: uppercase;
      color: #333;
    }

    .info-value {
      font-size: 18px;
      line-height: 1.2;
    }

    .event-description {
      margin: 0;
      font-size: 16px;
      line-height: 1.5;
    }

    .guest-header {
      display: flex;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }

    .guest-header h2 {
      margin: 0;
      font-size: clamp(20px, 2.5vw, 28px);
      text-transform: uppercase;
    }

    .guest-count {
      padding: 6px 12px;
      border: 3px solid var(--ink);
      box-shadow: 4px 4px 0 0 var(--ink);
      background: var(--beige);
      text-transform: uppercase;
      font-size: 13px;
      letter-spacing: 0.1em;
    }

    .guest-list {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-direction: column;
      gap: 12px;
      overflow-y: auto;
    }

    .guest-card {
      border: 3px solid var(--ink);
      padding: 14px;
      box-shadow: 6px 6px 0 0 var(--ink);
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .guest-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .guest-pill {
      border: 3px solid var(--ink);
      padding: 6px 10px;
      background: var(--mint);
      text-transform: uppercase;
      font-size: 11px;
      letter-spacing: 0.08em;
      box-shadow: 4px 4px 0 0 var(--ink);
    }

    .guest-name {
      font-size: 18px;
      text-transform: uppercase;
    }

    .guest-note {
      margin: 0;
      font-size: 14px;
      line-height: 1.4;
      color: #333;
    }

    .guest-card.empty {
      text-align: center;
      background: var(--beige);
    }

    .rsvp-form {
      margin-top: 24px;
      border: 3px solid var(--ink);
      padding: 18px;
      background: var(--paper);
      box-shadow: 6px 6px 0 0 var(--ink);
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .rsvp-title {
      margin: 0;
      font-size: clamp(18px, 2vw, 24px);
      text-transform: uppercase;
    }

    .form-field {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .form-input {
      border: 3px solid var(--ink);
      padding: 10px;
      font-size: 16px;
      background: #fff;
      box-shadow: 4px 4px 0 0 var(--ink);
    }

    .form-field-centered .info-label {
      text-align: center;
    }

    .optional-note {
      font-size: 11px;
      text-transform: none;
      letter-spacing: normal;
      color: #333;
    }

    .rsvp-choices {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      justify-content: center;
    }

    .choice-wrapper {
      position: relative;
    }

    .choice-input {
      position: absolute;
      opacity: 0;
      pointer-events: none;
    }

    .choice-label {
      border: 3px solid var(--ink);
      padding: 10px 22px;
      background: var(--mint);
      box-shadow: 4px 4px 0 0 var(--ink);
      text-transform: uppercase;
      font-size: 14px;
      cursor: pointer;
      display: inline-block;
    }

    .choice-input:checked + .choice-label {
      background: var(--beige);
    }

    .submit-button {
      border: 3px solid var(--ink);
      background: var(--mint);
      text-transform: uppercase;
      font-weight: bold;
      padding: 12px;
      font-size: 16px;
      box-shadow: 6px 6px 0 0 var(--ink);
      cursor: pointer;
      align-self: center;
    }

    .submit-button:active {
      box-shadow: 4px 4px 0 0 var(--ink);
      transform: translate(2px, 2px);
    }

    .submit-button:hover {
      background: var(--beige);
    }

    @media (max-width: 991.98px) {
      .event-pane {
        border-right: none;
        border-bottom: 4px solid var(--ink);
      }

      .guest-list {
        max-height: none;
      }
    }
  </style>
</head>

<body>
  <div class="shell">
    <header>
      <div class="brand">
        <h1>Partyless</h1>
      </div>
      <div class="tag">RSVPs without the BS</div>
    </header>

    <form id="uuid-form" style="display: none;">
      <input type="hidden" name="uuid" id="uuid-field" />
    </form>

    <main>
      <section class="card event-card">
        <div class="card-pane event-pane">
          <div>
            <h2 class="event-title">{{event_name}}</h2>
          </div>

          <div class="info-grid">
            <div class="info-block">
              <span class="info-label">Created by</span>
              <span class="info-value">{{hosts_name}}</span>
            </div>
            <div class="info-block">
              <span class="info-label">Location</span>
              <span class="info-value">{{address}}</span>
            </div>
          </div>

          <div class="info-block when-block">
            <span class="info-label">When</span>
            <p class="event-description">{{time}}</p>
          </div>

          <div class="info-block description-block">
            <span class="info-label">Details</span>
            <p class="event-description">{{description}}</p>
          </div>
        </div>

        <div class="card-pane guest-pane">
          <div class="guest-header">
            <h2>Guest List</h2>
            <span class="guest-count">{{guest_count}} RSVPs</span>
          </div>

          <ul class="guest-list">
            {{#guests}}
            <li>
              <article class="guest-card">
                <div class="guest-name">{{name}}</div>
                {{#note}}
                <p class="guest-note">{{.}}</p>
                {{/note}}
                <div class="guest-meta">
                  {{#status}}
                  <span class="guest-pill">{{.}}</span>
                  {{/status}}
                </div>
              </article>
            </li>
            {{/guests}}
            {{^guests}}
            <li>
              <article class="guest-card empty">
                Be the first to RSVP!
              </article>
            </li>
            {{/guests}}
          </ul>

          <form class="rsvp-form" hx-post="/rsvp" hx-include="[name='uuid']" hx-on::after-request="this.reset()" id="rsvp-form">
            <h3 class="rsvp-title">RSVP</h3>
            <label class="form-field">
              <span class="info-label">Name</span>
              <input class="form-input" type="text" name="name" placeholder="Your name" required />
            </label>

            <label class="form-field">
              <span class="info-label">Password <span class="optional-note">(optional)</span></span>
              <input class="form-input" type="password" name="password" placeholder="Set a password to edit later" />
            </label>

            <div class="form-field form-field-centered">
              <span class="info-label">Will you attend?</span>
              <div class="rsvp-choices">
                <label class="choice-wrapper">
                  <input class="choice-input" type="radio" name="response" value="yes" required />
                  <span class="choice-label">Yes</span>
                </label>
                <label class="choice-wrapper">
                  <input class="choice-input" type="radio" name="response" value="no" />
                  <span class="choice-label">No</span>
                </label>
                <label class="choice-wrapper">
                  <input class="choice-input" type="radio" name="response" value="maybe" />
                  <span class="choice-label">Maybe</span>
                </label>
              </div>
            </div>

            <button class="submit-button" type="submit">Send RSVP</button>
          </form>

        </div>
      </section>
    </main>
  </div>
</body>
</html>
